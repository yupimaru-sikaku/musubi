<%= render partial: 'shared/company_header' %>

<nav class="company_show_content">
    <nav class="home_point_container">

        <div class="home_point_title">
            <p><%= current_company.agency_name %> 様</p>
            <p>獲得ポイント一覧</p>
        </div>

        <div class="module-spacer--medium"></div>

        <% if @points.present? %>
            <div class="home_point_items">
                <% @points.each do |point| %>
                    <div class="home_point_item">
                        <p><%= point.point_type %></p>
                        <p><%= point.count %>pt</p>
                    </div>
                <% end %>
            </div>
        <% else %>
            <div class="home_point_title">
                <p>まだポイントを獲得しておりません</p>
            </div>
        <% end %>
        </div>

        <div class="module-spacer--medium"></div>
        
    </nav>
</nav>